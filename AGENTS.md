# AGENTS.md

이 문서는 본 프로젝트(및 본 빌더가 운영하는 모든 프로젝트)에서 AI 코딩 에이전트(Claude, Claude Code 등)와 인간 협업자가 따라야 하는 작업 규약이다. 모든 결정, 구현, 문서화, 커뮤니케이션은 이 규약을 기준으로 한다.

에이전트는 매 세션 시작 시 본 문서와 해당 프로젝트의 docs/definitions/(워크스페이스 내부에 있음), aiden/E&C/{프로젝트} (워크스페이스 바깥에 있음)/, aiden/L&C/{프로젝트} (워크스페이스 바깥에 있음)/를 먼저 일독한 뒤 작업에 진입한다.

## 0. 메타 원칙 (Meta Principles)

### 0.1 치열함의 원칙

- 깊게 고민해야 할 문제에는 모든 것을 쏟아붓는다. 표면적 답으로 넘기지 않는다.
- "대충 이 정도면 되겠다"는 결정 직후가 가장 위험한 순간이다. 그 순간에 한 번 더 파고든다.
- 결정 근거가 단순한 직관이라면, 그 직관을 분해해서 명시적 기준으로 만든 뒤 다시 결정한다.

### 0.2 가역성의 원칙 (Reversibility)

- 비가역(Irreversible) 결정은 가장 먼저, 가장 신중히 한다.
  - Backend 스키마, 데이터 모델, 인증·권한 구조, 전체 앱 아키텍처, 외부 API 의존성, 결제 흐름 등.
- 가역(Reversible) 결정은 마지막에, 한꺼번에 처리한다.
  - UI 위치, 색상, 카피, 버튼 라벨, 아이콘 등.
- 가역 사안에 시간을 너무 일찍 쓰면, 비가역 결정의 검토 시간을 잠식한다.

### 0.3 추적성의 원칙 (Traceability)

- 모든 오류는 aiden/E&C/에 기록한다.
- 모든 (오류가 아닌) 변경·학습은 aiden/L&C/에 기록한다.
- 동일한 실수를 두 번 하지 않는다. 문제 해결 전에 반드시 과거 케이스를 먼저 읽는다.
- 미래의 자신과 미래의 다른 프로젝트가 이 기록의 1차 독자다.

### 0.4 자율 검증의 원칙 (Self-Verification)

- 모든 구현은 그 자체로 단독 테스트가 가능한 진입점을 코드 안에 남긴다.
- 실기기 테스트에서 처음 발견되는 오류는 실패다. 그 이전 단계에서 잡혔어야 한다.
- Computer use, 시뮬레이터, 단위 테스트로 잡을 수 있는 오류는 거기서 끝낸다.

### 0.5 협업의 원칙 (Collaboration)

- 새로운 구현·기능은 무조건 사용자와 상의한다. 에이전트가 단독으로 결정하지 않는다.
- 상의 전에 반드시 해당 프로젝트의 E&C, L&C를 읽고 컨텍스트를 확보한다.
- 상의는 "어떻게 할까요?"가 아니라 "선택지 A/B/C가 있고, 각각의 장단점은 …이며, 저의 추천은 B입니다. 이유는 …" 형태로 한다.

## 1. 프로젝트 착수 전 (Pre-Project Phase)

### 1.1 기획 무결성 검증

사용자가 제시한 기획에 온전치 않은 부분이 있으면 주저 없이 질문한다. 모호한 채로 구현에 들어가지 않는다.

- 모호한 요구사항, 충돌하는 요구사항, 빠진 요구사항을 모두 식별한다.
- /grill-me(또는 동등한 적대적 질의 모드)를 적극 활용한다.
- 모든 구멍이 메워질 때까지 멈추지 않는다.
- 질문을 통해 도출된 답변은 테마별 별개 문서로 기록한다. 이 문서 묶음이 곧 §1.2의 Definition Documents가 된다.

### 1.2 결정해야 할 문서 묶음 (Definition Documents)

다음 8종 문서는 본격 구현 진입 전에 치열한 토론을 통해 결정·작성된다. 모두 docs/definitions/ 아래에 위치한다.

| # | 파일 | 역할 |
|---|---|---|
| 00 | 00-when-to-done.md | 완료 조건 (When to Done) |
| 01 | 01-logic-spec.md | 결과물 상세 설명 — 로직 |
| 02 | 02-ui-spec.md | 결과물 상세 설명 — UI |
| 03 | 03-product-spec.md | 제품으로서의 설명·목적 |
| 04 | 04-tech-stack.md | 채택 기술 및 스택 |
| 05 | 05-config-and-secrets.md | 필요한 설정값·API·외부 주입 요소 |
| 06 | 06-test-strategy.md | 테스트 방법·전략 |
| 07 | 07-phase-plan.md | 구현 Phase 구분·순서·계획 |
| 08 | 08-checklist.md | 전체 Phase 구현 체크리스트 |

#### 1.2.1 00-when-to-done.md — 완료 조건

- 무엇이 갖춰지면 "구현 완료"라 볼 수 있는가?
- 정량 조건(예: 명시된 구현해야 할 플로우 3개 모두 통과, 크래시율 0%, 첫 화면 LCP < 2s)과 정성 조건(예: 신규 사용자가 5초 내 핵심 기능 도달)을 모두 명시.
- 완료 조건은 후행적으로 추가·완화하지 않는다. 착수 전 합의 사항이다.

#### 1.2.2 01-logic-spec.md — 로직 명세

- 핵심 비즈니스 로직, 상태 전이, 데이터 흐름.
- 의사 코드, 시퀀스 다이어그램, 상태 머신 등으로 표현.
- 목표 수준: 이 기능을 처음 듣는 다른 개발자가 읽고 동일하게 구현할 수 있을 정도.

#### 1.2.3 02-ui-spec.md — UI 명세

- 화면 단위 구조, 주요 컴포넌트, 인터랙션, 네비게이션.
- 와이어프레임 수준이면 충분. 픽셀 단위 디자인이 아닌 구조와 의도를 명시.
- UI의 위치·색상 등 가역적 세부는 의도적으로 상세화하지 않는다.

#### 1.2.4 03-product-spec.md — 제품 명세

- 이 결과물은 무엇인가? 누구를 위한 것인가? 어떤 문제를 해결하는가?
- 기존 유사 제품과의 차별점.
- "제품"으로서의 존재 이유와 가치 제안.

#### 1.2.5 04-tech-stack.md — 기술 스택

- 언어, 프레임워크, 라이브러리, 인프라, DB, 외부 서비스.
- 각 선택의 근거와 검토한 대안, 탈락 사유.
- 의존성 버전 핀 명시.

#### 1.2.6 05-config-and-secrets.md — 설정·외부 주입

- 필요한 환경 변수, API 키, OAuth 클라이언트, 외부 서비스.
- 각 항목의 획득 방법, 권한 범위, 만료·갱신 주기.
- 실제 값은 절대 커밋하지 않는다. .env.example 또는 placeholder만 커밋.

#### 1.2.7 06-test-strategy.md — 테스트 전략

- 단계 구분: 단위 → 통합 → 시뮬레이터/Computer use 자율 → 실기기.
- 각 단계에서 잡아야 하는 오류 유형 명시.
- 실기기 테스트 진입 전까지 자체 코드 내부 테스트·시뮬레이터·Computer use로 잡아내야 할 오류 목록과 검증 방법.

#### 1.2.8 07-phase-plan.md — 구현 Phase 및 순서

- Phase 1 / Phase 2 / … 로 구분.
- 각 Phase의 산출물, 의존 관계, 예상 일정.
- 가역성 원칙에 따라 비가역 항목을 앞 Phase에 배치한다.

#### 1.2.9 08-checklist.md — 전체 Phase 체크리스트

- 모든 Phase의 모든 작업을 체크박스 형태로 나열.
- 진행 중 실시간 업데이트.
- 완료 시점에 00-when-to-done.md의 조건과 1:1 매핑되어야 한다.

### 1.3 레포지토리 셋업

- 모든 프로젝트는 시작과 동시에 Private GitHub 레포지토리를 생성한다.
- 초기 커밋에 AGENTS.md, README.md, docs/definitions/ 8종이 포함되어야 한다.
- .gitignore에 비밀 정보 파일, 빌드 산출물, 로컬 캐시 포함.
- 1인 프로젝트라도 메인 브랜치 보호(직접 push 금지, PR 필수)를 권장.

## 2. 구현 순서 (Build Order)

### 2.1 비가역 우선 — 권장 순서

1. 데이터 모델 & 스키마 — 변경 시 마이그레이션 비용이 크다.
2. 인증 & 권한 구조 — 후행 변경은 모든 엔드포인트에 파급된다.
3. Backend 핵심 로직 & API 계약 — 클라이언트의 모든 흐름이 의존한다.
4. 전체 앱 구조 & 네비게이션 골격 — 화면 추가가 아닌 구조 변경은 비싸다.
5. 핵심 비즈니스 플로우 (Happy Path) — 한 줄로 동작하는 가장 가는 줄기부터.
6. 예외·에러 처리 — Happy Path 위에 얹는다.
7. UI 세부·위치·색상·카피·아이콘 — 마지막에 한꺼번에.

### 2.2 UI 세부의 일괄 처리

UI 위치 등 언제든 바꿀 수 있는 요소는:

1. 마지막 단계에 Computer use로 모든 화면을 점검.
2. 발견된 모든 항목을 한 번에 기록(체크리스트화).
3. 그 후 하나씩 수정. 즉흥적·산발적 수정 금지.

이 원칙을 어기면 가역 사안 처리가 비가역 사안의 검토 시간을 잠식한다.

## 3. 테스트 전략

### 3.1 단독 테스트 가능성

- 모든 구현은 그 구현만 독립적으로 호출·검증할 수 있는 진입점을 코드 안에 남긴다.
- 예시: 함수 단위 테스트, 디버그 메뉴 진입점, #if DEBUG 트리거, CLI 명령어, Playground.
- "전체 앱을 빌드하고 화면 5개를 거쳐야만 검증 가능한 기능"은 설계 실패다.

### 3.2 자율 테스트 (Computer Use)

- Computer use로 지속적인 자율 테스트를 돌린다. 자잘한 오류, 회귀(regression), UI 깨짐을 선제적으로 잡는다.
- 자율 테스트가 잡은 오류는 즉시 aiden/E&C/에 기록한다.
- 매 PR 또는 매 의미 있는 커밋마다 자율 테스트 사이클을 1회 이상 돌리는 것을 강제.

### 3.3 실기기 테스트

- 실기기 테스트는 마지막 검증이지 첫 검증이 아니다.
- 실기기에서만 발견되는 오류는 최소화한다(센서·하드웨어 의존 항목 제외).
- 실기기 진입 전에 시뮬레이터·Computer use 단계의 회귀 테스트가 모두 통과해야 한다.

## 4. 문서화 규약 (Documentation Conventions)

### 4.1 코드 자산 명세 (Code Asset Spec)

타 프로젝트에서 재사용 가능한 코드 자산은 반드시 명세를 작성한다.

- 위치: docs/specs/
- 파일명 규칙: {프로젝트이름}-{하위요소}.md
- 포함 내용:
  - 무엇을 하는 자산인가
  - 입력·출력 인터페이스 (시그니처, 타입)
  - 내부 의존성과 외부 의존성
  - 사용 예제 (최소 1개의 동작하는 스니펫)
  - 알려진 한계·예외 케이스
  - 다른 프로젝트로 이식할 때의 체크포인트

### 4.2 오류 추적: aiden/E&C/ (Errors & Corrections)

디렉터리 구조:

```txt
aiden/
└── E&C/
    └── {프로젝트이름}/
        └── YYYY-MM-DD-{짧은-이슈-이름}.md
```

작성 절차:

1. 오류 발견 또는 보고 시 즉시 파일 생성.
2. 먼저 동일 프로젝트의 기존 E&C 전체를 일독한다. 유사 케이스에서 실마리를 찾을 수 있는 것부터 시도.
3. 해결 과정 전체를 기록한다(시도하고 실패한 것까지).

문서 템플릿:

```md
# {이슈 제목}

- 발생 일시:
- 환경: (OS, 디바이스, 버전, 빌드 번호 등)
- 재현 절차:
  1.
  2.
  3.

## 증상

무엇이 어떻게 잘못 동작하는가. 에러 메시지, 스택 트레이스, 스크린샷.

## 시도한 해결책 (실패 포함)

1. **시도 1**: 무엇을 했는가
   → 결과:
   → 이유 추정:

## 최종 해결

- 어떤 변경이 결정적이었는가
- 핵심 코드 diff 또는 변경점

## 회고

- 왜 이 오류가 발생했는가 (근본 원인)
- 다음에 같은 유형의 오류를 피하려면

## 관련 자산

- 관련 코드 위치
- 관련 L&C 항목
```

### 4.3 변경 추적: aiden/L&C/ (Learnings & Changes)

오류는 아니지만 초안 대비 바뀐 사항·학습은 L&C에 기록한다.

디렉터리 구조:

```txt
aiden/
└── L&C/
    └── {프로젝트이름}/
        └── YYYY-MM-DD-{짧은-변경-이름}.md
```

무엇을 기록하는가:

- 처음에는 필요하다고 봤지만 실제로는 필요 없었던 것.
- 처음에는 충분하다고 봤지만 실제로는 과했던 것(over-engineering).
- 처음에는 예상하지 못했지만 앞으로의 프로젝트에 매우 유용할 것.
- 향후 프로젝트의 길잡이가 될 일반화 가능한 통찰.

문서 템플릿:

```md
# {변경 제목}

- 일자:
- 분류: 불필요 / 과했음 / 유용 / 통찰

## 상황 (Context)

- 처음에는 어떻게 계획·구현했는가
- 무엇을 가정했는가

## 결론에 도달한 경로 (Path to Conclusion)

- 어떤 관찰·이벤트가 재고를 일으켰는가
- 어떤 대안을 고려했는가
- 어떻게 결정했는가

## 결과 (Outcome)

- 최종적으로 어떻게 바뀌었는가
- 정량적·정성적 효과

## 일반화 (Generalization)

- 이 학습이 적용 가능한 미래의 상황 유형
- 다음 프로젝트에서의 체크포인트
```

## 5. 사용자 커뮤니케이션 프로토콜

### 5.1 새 구현·기능 상의

- 단독으로 결정하지 않는다.
- 상의 전에 해당 프로젝트의 E&C, L&C를 읽는다.
- 다음 포맷으로 상의한다:

```md
## 안건: {무엇을 결정해야 하는가}

## 관련 기존 기록
- E&C: (관련 케이스 요약 또는 "해당 없음")
- L&C: (관련 학습 요약 또는 "해당 없음")

## 선택지

### A. {선택지 이름}
- 장점:
- 단점:
- 비용 (시간·복잡도·의존성):

### B. {선택지 이름}
- 장점:
- 단점:
- 비용:

### C. {선택지 이름}
- …

## 추천 및 근거
- 추천: B
- 근거:

## 결정 요청
- 무엇에 대한 동의·반대를 요청하는가
```

### 5.2 모호함 발견 시

- 모호함을 발견한 즉시 멈춘다. 추측으로 채우지 않는다.
- 한 번에 1–3개 질문으로 추린다(너무 많은 질문 동시 발사 금지).
- 결정 후에는 docs/definitions/ 또는 aiden/L&C/에 즉시 반영.

## 6. Git / GitHub 규약

- 모든 프로젝트는 Private GitHub 레포지토리에서 관리.
- 프로젝트 시작 시 즉시 레포 생성, 초기 커밋에 AGENTS.md 포함.
- 커밋 메시지 접두 권장: feat:, fix:, docs:, refactor:, test:, chore:.
- 비가역 결정 직전에는 별도 브랜치를 만들어 작업한다.
- 비밀 정보(API 키, 토큰, 인증서)는 절대 커밋하지 않는다.

## 7. 부록

### 7.1 표준 디렉터리 구조

```txt
project-root/
├── AGENTS.md
├── README.md
├── .gitignore
├── .env.example
├── docs/
│   ├── definitions/
│   │   ├── 00-when-to-done.md
│   │   ├── 01-logic-spec.md
│   │   ├── 02-ui-spec.md
│   │   ├── 03-product-spec.md
│   │   ├── 04-tech-stack.md
│   │   ├── 05-config-and-secrets.md
│   │   ├── 06-test-strategy.md
│   │   ├── 07-phase-plan.md
│   │   └── 08-checklist.md
│   └── specs/
│       └── {프로젝트}-{하위요소}.md
└── src/

~/aiden/
├── E&C/
│   └── {프로젝트}/
│       └── YYYY-MM-DD-{이슈}.md
└── L&C/
    └── {프로젝트}/
        └── YYYY-MM-DD-{변경}.md
```

### 7.2 에이전트가 매 세션 시작 시 수행할 일

1. AGENTS.md (본 문서) 일독.
2. 해당 프로젝트의 docs/definitions/ 전체 일독.
3. 해당 프로젝트의 aiden/E&C/{프로젝트}/, aiden/L&C/{프로젝트}/ 일독.
4. 현재 진행 위치 확인: docs/definitions/08-checklist.md.
5. 진행할 작업이 비가역인지 가역인지 분류. 비가역이면 사용자 상의부터.

### 7.3 빠른 의사결정 체크리스트

새 작업을 시작하기 전 5초 안에 점검한다:

- [ ] 이 작업은 비가역적인가? Yes면 사용자 상의부터.
- [ ] 관련 E&C, L&C를 읽었는가?
- [ ] 단독 테스트 진입점을 함께 구현하는가?
- [ ] 완료 후 08-checklist.md를 업데이트하는가?
- [ ] 오류 발생 시 즉시 E&C에 기록할 준비가 되어 있는가?
- [ ] 초안 대비 변경이 발생하면 L&C에 기록할 준비가 되어 있는가?

### 7.4 자주 묻는 판단 기준

| 상황 | 판단 |
|---|---|
| "이거 그냥 빨리 고치고 넘어갈까?" | E&C에 기록한 뒤 고친다. 기록 없는 수정은 부채. |
| "이건 너무 사소해서 사용자에게 안 물어봐도 될 듯" | 비가역이면 무조건 묻는다. 가역이고 명세 범위 내라면 진행해도 됨. |
| "테스트는 나중에 한꺼번에" | 안 됨. 구현과 동시에 단독 테스트 진입점을 만든다. |
| "이 변경은 너무 사소해서 L&C에 안 적어도 될 듯" | "사소함"이 일반화 가능한 통찰을 포함한다면 기록한다. 형식보다 패턴이 중요. |

본 문서는 살아있는 문서다. 새로운 학습이 일반화 가능하다고 판단되면 본 문서에 흡수한다. 흡수 시점·내용은 aiden/L&C/에 기록한다.
